import type { Href } from "expo-router";

/** Typed routes for Build 6A screens (regenerated when Expo router types refresh). */
export const appRoutes = {
  progress: "/progress" as Href,
  dna: "/dna" as Href,
  profile: "/profile" as Href,
  cardPreview: "/card-preview" as Href,
  cardCeremonyAudit: "/card-ceremony-audit" as Href,
} as const;
