 FairFrame – Live Chief Integration PromptFairFrame is still using offline mock data. I want to connect the app to a real OpenAI-powered Chief analysis.Please inspect the current analysis pipeline and identify where mock/demo data is being returned.Then implement a live analysis path:1. Capture or upload image in the app.2. Convert the image to a format that can be sent to a backend function.3. Create a secure backend function, preferably Supabase Edge Function, called analyze-frame.4. Store the OpenAI API key only on the backend, never in the mobile app.5. Send the image and optional story context to OpenAI.6. Use the Chief Constitution / FairFrame Founder Bible as the system instructions.7. Return structured JSON containing: - scene - visibleObjects - visualHierarchy - chiefsFirstImpression - visualStorytellingScore - potentialScore - pillarScores - whatChiefSees - strengths - improvements - recommendations8. Replace the offline mock response with the live response.9. Keep a fallback mock response only if the live request fails.10. Clearly label whether the report is LIVE CHIEF or OFFLINE PREVIEW.Before coding, explain the current pipeline and your implementation plan.